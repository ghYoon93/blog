spring:
  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:blog
      username: sa
      password:
      driver-class-name: org.h2.Driver

  data:
    web:
      pageable:
        one-indexed-parameters: true